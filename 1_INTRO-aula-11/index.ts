const firstName = 'Casemiro';
const anotherName = 1;
const x = true;

const greeting = (name: string) => {
  console.log('Hello ' + name);
};

greeting(firstName);
// greeting(anotherName);
// greeting(x);
